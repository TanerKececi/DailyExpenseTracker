package com.example.dailyexpensetracker.domain.model

enum class TransactionType { EXPENSE, INCOME }

enum class TransactionStatus { PAID, OVERDUE, UPCOMING }

enum class CardType { VISA, MASTERCARD }
